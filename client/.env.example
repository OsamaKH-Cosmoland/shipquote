# No env vars are required for the frontend.
# API calls go to /api/* and are proxied to the backend:
#   - production: client/vercel.json rewrites /api/* to the backend deployment
#   - local dev:  vite.config.ts proxies /api/* to http://localhost:3000
# To point local dev at a different backend, edit the proxy target in vite.config.ts.
